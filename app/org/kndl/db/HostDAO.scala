package org.kndl.db

import play.api.db.DB
import anorm._

/**
 *
 *
 *
 */
class HostDAO {
  /*val db = DB.getDataSource()

  def getHost(id:Int) {
    DB.withConnection { implicit c =>
      val result: Boolean = SQL("select * from host where id = {id}")
                            .on("id" -> id).execute()
    }
  }  */
}
